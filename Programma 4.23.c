#include <stdio.h>
#include <math.h>

int main() {
    
    int num, risultato;
    
    num = 144;
    risultato = sqrt(num);
    printf("sqrt(%d)= %d", num, risultato);
    
    return 0;
    
    }
